#To interact with data in records, use the syntax 
#arrayName[index].fieldName.

# To add 1000 records:
#for x in range(1000):
	#allPlayers[x].uniqueId = str(x)
	#allPlayers[x].score = int(input("What is the score?"))
	#allPlayers[x].minutes = int(input("How many minutes?"))
# ... other fields
	
# To display data:
#print(allPlayers[0].uniqueId)
#print(allPlayers[0].score)
#print(allPlayers[1].minutes)