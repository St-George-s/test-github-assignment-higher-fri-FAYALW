# List of usernames
username = ['@figgytea', '@deepsv', '@selmialwan']

# List of corresponding followers for each username
followers = [3500, 6700, 8248]

# Loop through the range of the length of the username list
for count in range(len(username)):
    # Print the username at the current index
    print(username[count])
    
    # Print the corresponding number of followers at the same index
    print(followers[count])
