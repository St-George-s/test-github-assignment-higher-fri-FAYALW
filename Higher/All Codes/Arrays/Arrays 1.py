# Create an array (list) with values from 1 to 5
array = [1, 2, 3, 4, 5]
# Print the entire array to the console
print(array)

# Create another array (list) with values 10, 20, 30, 40, and 50
array = [10, 20, 30, 40, 50]
# Print the first and last elements of the array (index 0 and index 4)
print(array[0], array[4])